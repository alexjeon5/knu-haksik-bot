services:
  knu-bot:
    build: .
    container_name: knu_menu_bot
    restart: always
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Seoul
    logging:
      driver: "json-file"
      options:
        max-size: "10mb"
        max-file: "3"