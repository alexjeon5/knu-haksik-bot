services:
  knu-bot:
    build: .
    container_name: knu_menu_bot
    restart: always
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
    logging:
      driver: "json-file"
      options:
        max-size: "10mb"
        max-file: "3"